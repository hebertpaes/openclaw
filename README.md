# openclaw

Projeto inicial do repositório `openclaw`.

## Estrutura

- `README.md`: visão geral do projeto
- `.gitignore`: arquivos/pastas ignorados pelo Git

## Próximos passos

1. Definir stack (Node, Python, Go etc.)
2. Criar estrutura de código em `src/`
3. Configurar testes e CI
